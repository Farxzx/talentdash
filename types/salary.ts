export interface SalaryRecord {
  id: string;
  company: string;
  slug: string;
  role: string;
  level: string;
  location: string;
  experience: number;
  baseSalary: number;
  bonus: number;
  stock: number;
  totalComp: number;
}